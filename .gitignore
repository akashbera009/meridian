.DS_Store
node_modules/
__pycache__/
.venv/
.env
*.log

# local git identity for this workspace — machine config, not project content
.gitconfig

# local scratch — the roadmap embeds the code, the folder stays local
example/
