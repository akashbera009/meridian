[project]
name = "04fast-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "akashbera009", email = "akashbera102003@gmail.com" }
]
requires-python = ">=3.11.9"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "ollama>=0.6.2",
    "sse-starlette>=3.4.11",
]

[project.scripts]
04fast-api = "04fast_api:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
